# 「我的足迹」使用与权限设置指南

> 适用版本：v4.6（我的足迹-v4.6.apk）· 被动记录版：装好后**不用打开也会自动记录**，
> 每 15 分钟记一个点，点连成线就是你走过的路。

---

## 一、安装 App

1. 把电脑上的 `我的足迹-v4.6.apk` 发到手机：微信「文件传输助手」、QQ、或数据线拷贝都可以
2. 在手机上点开这个文件，系统提示「禁止安装未知应用」时：
   点 **设置 → 允许来自此来源的应用**（开关打开）→ 返回继续安装
3. 如果手机上已经装过旧版本，直接覆盖安装即可（不用卸载，数据不会丢）

---

## 二、首次打开与权限设置（最关键的一步！）

权限设置直接决定「熄屏后还能不能自动记录」。按下面顺序走：

### 第 1 步：隐私说明

首次打开会弹出「服务与隐私说明」→ 点 **同意并继续**。
（不同意的话地图无法使用，App 会退出）

### 第 2 步：定位权限（系统弹窗）

- 弹出定位权限申请时，选择 **「精确位置」**（不要选"大致位置"）
- 如果系统问授权时机，尽量选 **「始终允许」**；
  如果只有「使用时允许」可选，就先选它，下一步再改

### 第 3 步：后台定位（Android 10 以上手机必做）

同意定位权限后，App 会再次弹窗申请**后台定位**：

- 系统会跳到一个设置页，把定位改为 **「始终允许」**
- 这一步是「熄屏/退出 App 后仍能记录」的关键，**必须选"始终允许"**
- 如果不小心选了"仅使用时允许"：去 **系统设置 → 应用管理 → 我的足迹 → 权限 → 位置信息 →
  改为"始终允许"**

### 第 4 步：电池优化白名单

之后会弹出一个系统页面问「是否允许忽略电池优化」→ 点 **允许**。

> 这个弹窗只会出现一次。如果当时点错了：
> 系统设置 → 应用 → 特殊应用权限 → 电池优化 → 找到「我的足迹」→ 改为「不优化/无限制」。

### 第 5 步：国产手机的额外保命设置（小米/华为/OPPO/vivo 必做）

国产系统会激进杀后台，不做这步可能熄屏一段时间后就不记点了：

| 手机品牌 | 设置路径 |
|---------|---------|
| **小米/红米** | 设置 → 应用设置 → 应用管理 → 我的足迹 → **省电策略 → 无限制**；同一页打开**自启动** |
| **华为/荣耀** | 设置 → 应用 → 应用启动管理 → 找到「我的足迹」→ 关闭「自动管理」→ 手动打开**自启动、关联启动、后台活动**三个开关 |
| **OPPO/一加** | 设置 → 应用管理 → 我的足迹 → **允许自启动**、**允许后台运行**；电池 → 更多 → 允许完全后台行为 |
| **vivo** | 设置 → 电池 → 后台高耗电 → 允许「我的足迹」；设置 → 更多设置 → 应用管理 → 自启动管理 → 打开 |
| **三星** | 设置 → 电池 → 后台使用限制 → 把「我的足迹」从"受限应用"移到"不受限制" |

**再加一道保险**：打开最近任务（多任务）界面，找到「我的足迹」的卡片，**下拉/长按卡片点锁头图标 🔒**，
这样手动清后台时它不会被清掉。

---

## 三、怎么确认它在正常工作？

1. 装好权限设置完，打开 App，右下角信息卡会显示：
   **「今日已记录 1 个轨迹点」** —— 因为每次打开 App 都会立即补记一个点
2. 等 20～40 分钟后再打开（或熄屏放着再来看），数字应该变成 2 或 3
3. 数字在涨 = 一切正常，以后就不用管它了

**如果数字一直是 0 / 不涨**，按顺序检查：
① 系统设置 → 应用 → 我的足迹 → 权限 → 位置信息是否为「始终允许」
② 上面的第 5 步（省电策略/自启动）是否做了
③ 手机的"省电模式/超级省电"是否开着（开着会冻结后台，先关掉试试）
④ 重新打开一次 App（会立即补一个点，数字+1 就说明记录链路是通的，问题只在后台保活）

---

## 四、日常使用

App 平时完全不用打开，自动在后台记。想看足迹时打开：

- **底部页签**：一生 / 今日 / 昨日 / 七日 / 此月 —— 点哪个，地图上的线就显示哪个时间段的足迹
- **顶部「运动模式」按钮**：想记录完整行进路线时点它！切换成连续定位（2 秒一个点），
  地图实时画出你走的每一段路，右下角显示实时距离和均速；到地方再点一次「停止」即可。
  代价是连续 GPS 比日常模式费电（约每小时 5~10%），平时不用开
- **左上角 ☰ 日志按钮**：弹出轨迹列表，每条显示「几点几分 + 当时所在的地址」；
  点某一条，地图就放大居中到那个点（橙色高亮），方便回忆"那个时间我在哪"。
  标题右侧红字「清空全部」可删除全部轨迹重新开始（需两次确认，不可恢复）
- **右下角信息卡**：日常显示今日点数 + 累计总里程；运动中显示实时距离/点数/均速
- **右上角按钮**：切换暗黑 ↔ 亮色主题（暗黑是黑色地图）
- 地图可以用手指正常拖动、双指缩放

**预期效果**：
- 每次打开 App 首先看到的是「**今日**」页
- 「一生」页：**只显示红色小圆点**——你一生到过的所有地方一目了然
- 「今日 / 昨日 / 七日 / 此月」页：显示**红点 + 按顺序连成的红色细线**（行进轨迹）
- 一直待在原地时距离不增加（防漂移设计）；走出门线就"长"出来了
- 想记录完整行进路线时开顶部「运动模式」
- 想让某个时段记录更密？打开 App 即记一点；连续开着 App 时依然按 15 分钟节奏走

---

## 五、重要注意事项

1. **数据全部存在手机本地**：卸载 App = 轨迹全部清空，换手机数据不会跟着走
   （导出/备份功能在开发计划里，目前还没有）
2. **地图瓦片需要联网**加载，轨迹记录本身离线也能工作（GPS 不需要网络）
3. **室内记的点会有偏差**：室内没有 GPS 卫星信号，只能靠 WiFi/基站定位。App 已采用
   「开窗择优」策略（连续定位 15 秒取最准一次）+ 传感器辅助，实测室内精度可从 550 米级
   提升到 75 米级；户外 GPS 可达 10~30 米。想再好一点：系统设置 → 位置信息 →
   打开「WLAN 扫描」和「蓝牙扫描」；保持 WiFi 开启（不用连接也行）
4. **省电情况**：每 15 分钟唤醒一次定位几秒钟，正常使用一天额外耗电约 1%～3%
5. **升级新版**：直接安装新 APK 覆盖即可，数据和权限都保留

---

## 六、快速排障表

| 现象 | 原因与解法 |
|------|-----------|
| 地图是空白格子 | 高德 Key 绑定信息不对（需包名 `com.footprints.app` + SHA1 `5B:2B:26:D5:51:DD:E8:8D:61:80:18:1B:E9:4A:2A:30:BD:DA:8C:CE`），或手机没网 |
| 信息卡数字不涨 | 权限不是"始终允许"，或被省电策略冻结 → 见上面第 2、3、5 步 |
| 熄屏一晚回来只记了几个点 | 国产 ROM 杀后台 → 补做第 5 步 + 最近任务加锁 🔒 |
| 线段乱跳/飘 | 那段处于室内或信号差区，精度过滤已经挡掉了最差的部分 |
| 时间显示不对 | App 用的是 GPS 时间戳，与手机系统时间无关，正常 |
